# Arcane

A wizard-themed board game

Architecture: UI -> GameCommand -> Game -> GameEvent -> UI

Currently: Command-line prototype.

Planned: Prep/Battle cycles, knowledge, mana, potions, implements, spells, monsters, bosses.